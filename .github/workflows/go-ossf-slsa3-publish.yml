# This workflow uses actions that are not certified by GitHub.# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。# 此工作流使用了未经 GitHub 认证的操作。
# They are provided by a third-party and are governed by# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受# 它们由第三方提供，并受
# separate terms of service, privacy policy, and support# 独立的服务条款、隐私政策及支持政策约束
# documentation.

# This workflow lets you compile your Go project using a SLSA3 compliant builder.
# This workflow will generate a so-called "provenance" file describing the steps
# that were performed to generate the final binary.
# The project is an initiative of the OpenSSF (openssf.org) and is developed at# 该项目由 OpenSSF（openssf.org）发起，并在以下地址开发# 该项目由 OpenSSF（openssf.org）发起，并在以下地址开发
# https://github.com/slsa-framework/slsa-github-generator.
# The provenance file can be verified using https://github.com/slsa-framework/slsa-verifier.# 可通过 https://github.com/slsa-framework/slsa-verifier 验证溯源文件。# 可通过 https://github.com/slsa-framework/slsa-verifier 验证溯源文件。# 可通过 https://github.com/slsa-framework/slsa-verifier 验证溯源文件。# 可通过 https://github.com/slsa-framework/slsa-verifier 验证溯源文件。# 可通过 https://github.com/slsa-framework/slsa-verifier 验证溯源文件。# 可通过 https://github.com/slsa-framework/slsa-verifier 验证溯源文件。# 可通过 https://github.com/slsa-framework/slsa-verifier 验证溯源文件。# 可通过 https://github.com/slsa-framework/slsa-verifier 验证溯源文件。
# For more information about SLSA and how it improves the supply-chain, visit slsa.dev.# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。# 有关 SLSA 及其如何提升供应链安全的更多信息，请访问 slsa.dev。

name: : SLSA Go releaserSLSA Go 发布工具
on::开启：:
  workflow_dispatch workflow_dispatch workflow_dispatch workflow_dispatch workflow_dispatch workflow_dispatch workflow_dispatch workflow_dispatch:  workflow_dispatch：:
  release release: 发布：
    types: [created]    类型：[created]

permissions: read-all权限：read-all权限: 读取全部权限：read-all

jobs:作业：
  # ========================================================================================================================================
  #     Prerequesite: Create a .slsa-goreleaser.yml in the root directory of your project.  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。#     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。#     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。#     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。  #     先决条件：在项目根目录下创建一个 .slsa-goreleaser.yml 文件。
  #       See format in https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/go/README.md#configuration-file  #       参见 https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/go/README.md#configuration-file 中的格式  #       参见 https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/go/README.md#configuration-file 中的格式#       参见 https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/go/README.md#configuration-file 中的格式  #       参见 https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/go/README.md#configuration-file 中的格式  #       参见 https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/go/README.md#configuration-file 中的格式#       请参阅 https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/go/README.md#configuration-file 中的格式  #       请参阅 https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/go/README.md#configuration-file 中的格式  #       请参阅 https://github.com/slsa-framework/slsa-github-generator/blob/main/internal/builders/go/README.md#configuration-file 中的格式
  #=========================================================================================================================================
  build:  构建：
    permissions:    权限：
      id-token: write # To sign.      id-token：write # 用于签名。      id-token: write # 用于签名。# 用于签名。      id-token：write # 用于签名。# 用于签名。      id-token：write # 用于签名。      id-token: write # 用于签名。# 用于签名。      id-token：write # 用于签名。# 用于签名。      id-token：write # 用于签名。      id-token: write # 用于签名。# 用于签名。      id-token：write # 用于签名。# 用于签名。      id-token：write # 用于签名。      id-token: write # 用于签名。# 用于签名。      id-token：write # 用于签名。
      contents      内容: : write # To upload release assets.      contents: write # 用于上传发布资产。# 用于上传发布资产。编写 # To upload release assets.      contents: write # 用于上传发布资产。# 用于上传发布资产。编写 # To upload release assets.      contents: write # 用于上传发布资产。# 用于上传发布资产。编写 # To upload release assets.      contents: write # 用于上传发布资产。# 用于上传发布资产。# 用于上传发布资产。      内容：编写 # 用于上传发布资产。# 用于上传发布资产。编写 # 用于上传发布资产。      内容：编写 # 用于上传发布资产。# 用于上传发布资产。编写 # 用于上传发布资产。     内容：write # 用于上传发布资产。# 用于上传发布资产。# 用于上传发布资产。      内容：编写 # 用于上传发布资产。# 用于上传发布资产。编写 # 用于上传发布资产。      内容：编写 # 用于上传发布资产。# 用于上传发布资产。编写 # 用于上传发布资产。     contents: write # 用于上传发布资产。# 用于上传发布资产。# 用于上传发布资产。      内容：编写 # 用于上传发布资产。# 用于上传发布资产。编写 # 用于上传发布资产。      内容：编写 # 用于上传发布资产。# 用于上传发布资产。编写 # 用于上传发布资产。     内容：write # 用于上传发布资产。# 用于上传发布资产。
      actions: : read   # To read workflow path.      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。      actions: : read   # 用于读取工作流路径。      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。      actions: : read   # 用于读取工作流路径。      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。      actions: : read   # 用于读取工作流路径。      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。      actions: : read   # 用于读取工作流路径。      actions: read   # 用于读取工作流路径。# 用于读取工作流路径。      actions: read   # 用于读取工作流路径。
    uses: slsa-framework/slsa-github-generator/.github/workflows/builder_go_slsa3.yml@v1.4.0    使用: slsa-framework/slsa-github-generator/.github/workflows/builder_go_slsa3.yml@v1.4.0
    with:
      go-version: 1.17
      # =============================================================================================================
      #     Optional: For more options, see https://github.com/slsa-framework/slsa-github-generator#golang-projects#     可选：如需更多选项，请参阅 https://github.com/slsa-framework/slsa-github-generator#golang-projects      #     可选：如需更多选项，请参阅 https://github.com/slsa-framework/slsa-github-generator#golang-projects#     可选：如需更多选项，请参阅 https://github.com/slsa-framework/slsa-github-generator#golang-projects      #     可选：如需更多选项，请参阅 https://github.com/slsa-framework/slsa-github-generator#golang-projects#     可选：如需更多选项，请参阅 https://github.com/slsa-framework/slsa-github-generator#golang-projects      #     可选：如需更多选项，请参阅 https://github.com/slsa-framework/slsa-github-generator#golang-projects#     可选：如需更多选项，请参阅 https://github.com/slsa-framework/slsa-github-generator#golang-projects      #     可选：如需更多选项，请参阅 https://github.com/slsa-framework/slsa-github-generator#golang-projects
      # =============================================================================================================

